# SDK 优化改进清单

## 🔴 关键问题 (Critical)

- [x] **依赖检查缺失**：在执行 Gaussian Splatting 前需验证 SfM 是否已成功运行
  - 位置：`my_sdk/core/pipeline.py`
  - 已添加 `_validate_dependencies()` 方法

- [x] **`experiment_demo.py` API 未实现**：`pipeline.run(stages=["sfm"])` 分阶段执行功能不存在
  - 已实现 `run(stages=[...])` 参数支持

---

## 🟠 架构改进 (Architecture)

- [x] **日志系统**：将 `print()` 替换为 Python `logging` 模块
  - 已实现 `utils/docker_runner.py`，支持日志级别、文件保存、进度提取

- [ ] **进度回调机制**：添加进度报告回调
  ```python
  progress_callback: Optional[Callable[[str, float], None]] = None
  ```

- [ ] **输出产物管理**：
  - 为每种输出类型配置独立路径
  - 支持输出格式选择（PLY/OBJ/GLTF 等）

- [ ] **2D 输出步骤**：添加 `run_2d` 选项（如正射影像 DOM）

---

## 🟡 代码健壮性 (Robustness)

- [ ] **Docker 可用性检查**：
  - 验证 Docker 是否安装且运行
  - 检查所需镜像是否存在

- [ ] **图片格式匹配不完整**：`opensfm.py` 只匹配 `.jpg`
  - 位置：`my_sdk/adapters/opensfm.py:23`
  - 建议：支持 PNG/JPEG/jpeg 等更多格式

- [ ] **配置验证增强**：
  - 验证 `working_dir` 是否可写
  - 检查磁盘空间是否足够

---

## 🟢 功能增强 (Enhancement)

- [ ] **任务状态持久化**：支持断点续跑
  ```json
  {"completed_steps": ["sfm"], "failed_step": null}
  ```

- [ ] **Config Schema 导出**：提供 JSON Schema 供 IDE 自动补全

- [ ] **更多算法适配器占位**：
  - `adapters/colmap.py`
  - `adapters/meshroom.py`
