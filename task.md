我现在跑通了从多张照片到空三、二维、三维、高斯重建的整个流程。
使用的是odm+opensplat（两者都以docker存在和运行）
现在我想参照mipmap：https://docs.mipmap3d.com/engine/zh-Hans/api-reference/overview
实现自己的sdk。
要求：
1. 通过json配置任务；
2. 可以选择输出哪些重建结果：空三\2d\3d\高斯；
3. 我以后可能会替换算法，比如我把空三换成了colmap，所以sdk需要设计成方便替换算法的方式；
4. 不同的算法对应不同的参数，所以sdk需要设计成方便替换参数的方式；
5. 需要对异常做检查，比如空三失败了，2d失败了，3d失败了，高斯失败了，需要有异常处理机制；比如在不执行空三的时候执行高斯，需要有异常处理机制；
6. 相机参数，gps等也应该加入配置（参考mipmap的配置）；